# gRPC keeps default runtime initialization: Netty channel factories depend on the target platform.
# Protobuf descriptors have cyclic initialization dependencies; preserve JVM runtime ordering.
Args = --initialize-at-run-time=com.google.protobuf \
       --initialize-at-build-time=kotlin
