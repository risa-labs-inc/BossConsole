# Build output of the single-file Java harness (javac -d out ...).
out/
# Raw per-run results. Regenerated by every sweep and machine-specific, so the
# numbers that matter are the ones written up in WINDOWS.md instead.
results-win/
