# Use key=value syntax, lowercase platform names, and at most three components.
# macos is required by Gradle. Only macOS is currently published.
# Windows marketing versions/build floors need a suitable detector before use.
# Linux means kernel version; suffixed kernel releases currently fail open.
macos=13.0
